export enum PlatformEvent {
  USER_LOGGED_IN = 'platform.user.logged_in',
  USER_REGISTERED = 'platform.user.registered',
  AUDIT_LOG_CREATED = 'platform.audit.created',
  RESOURCE_MUTATED = 'platform.resource.mutated',
}
